# Radio Recorder Project - 아이디어 정리

## 프로젝트 개요

Ubuntu 서버 환경에서 Docker 컨테이너로 작동하는 헤드리스(headless) 라디오 방송 녹음 시스템. 물리적 사운드 디바이스 없이 스트리밍 라디오를 녹음하고, 웹 대시보드를 통해 관리 및 재생할 수 있는 통합 솔루션.

## 핵심 기능

### 1. 녹음 시스템
- **스트림 캡처**: FFmpeg/GStreamer를 활용한 다양한 라디오 스트림 프로토콜 지원 (HLS, RTMP, HTTP/HTTPS, MMS)
- **스케줄링**: Cron-like 스케줄러로 예약 녹음
- **동시 녹음**: 멀티 스레드/비동기 처리로 여러 방송 동시 녹음
- **메타데이터 수집**: 방송 제목, 프로그램 정보, 시간 태그 자동 수집

### 2. 오디오 처리
- **포맷 변환**: MP3, AAC, FLAC, OGG 등 다양한 포맷 지원
- **품질 설정**: 비트레이트, 샘플레이트 커스터마이징
- **후처리**: 무음 제거, 노멀라이제이션, 광고 감지 및 마킹
- **청크 분할**: 긴 녹음을 시간 단위로 자동 분할

### 3. AI 기능 확장
- **음성 인식**: Whisper API를 통한 자동 전사
- **콘텐츠 요약**: LLM을 활용한 방송 내용 요약
- **화자 분리**: Speaker diarization으로 화자별 구분
- **키워드 추출**: 중요 키워드 및 토픽 자동 태깅
- **감정 분석**: 방송 톤 및 감정 분석

### 4. 웹 대시보드
- **실시간 모니터링**: 현재 녹음 상태, CPU/메모리 사용량
- **녹음 관리**: 스케줄 CRUD, 즉시 녹음 시작/중지
- **라이브러리**: 녹음된 파일 브라우징, 검색, 필터링
- **플레이어**: 웹 기반 오디오 플레이어 (웨이브폼 시각화)
- **분석 대시보드**: 녹음 통계, 청취 패턴, 저장 공간 사용량

## 기술 스택

### Backend (Python FastAPI)
```
- FastAPI: 고성능 비동기 웹 프레임워크
- SQLAlchemy: ORM 및 데이터베이스 관리
- Celery + Redis: 비동기 작업 큐 (녹음, 변환, AI 처리)
- APScheduler: 스케줄링 관리
- FFmpeg-python: 오디오 스트림 처리
- Pydantic: 데이터 검증
- MinIO/S3: 오디오 파일 스토리지
```

### Frontend (React + TypeScript)
```
- Vite: 빌드 도구
- TanStack Router: 타입 세이프 라우팅
- Radix UI: 접근성 준수 UI 컴포넌트
- TanStack Query: 서버 상태 관리
- Zustand: 클라이언트 상태 관리
- Recharts: 데이터 시각화
- WaveSurfer.js: 오디오 웨이브폼
- Tailwind CSS: 스타일링
```

### Infrastructure
```
- Docker Compose: 멀티 컨테이너 오케스트레이션
- PostgreSQL: 메타데이터 저장소
- Redis: 캐시 및 작업 큐
- Nginx: 리버스 프록시
- Prometheus + Grafana: 모니터링 (선택적)
```

## 모노레포 구조

```
radio-recorder/
├── apps/
│   ├── backend/          # FastAPI 백엔드
│   │   ├── app/
│   │   │   ├── api/      # API 엔드포인트
│   │   │   ├── core/     # 핵심 비즈니스 로직
│   │   │   ├── models/   # 데이터베이스 모델
│   │   │   ├── services/ # 서비스 레이어
│   │   │   ├── workers/  # Celery 작업
│   │   │   └── utils/    # 유틸리티
│   │   └── tests/
│   │
│   └── frontend/         # React 프론트엔드
│       ├── src/
│       │   ├── components/
│       │   ├── pages/
│       │   ├── hooks/
│       │   ├── stores/
│       │   └── utils/
│       └── tests/
│
├── packages/             # 공유 패키지
│   ├── types/           # TypeScript 타입 정의
│   └── config/          # 공통 설정
│
├── docker/              # Docker 설정
│   ├── backend.Dockerfile
│   ├── frontend.Dockerfile
│   └── docker-compose.yml
│
├── scripts/             # 유틸리티 스크립트
└── docs/               # 문서화
```

## 주요 기술적 고려사항

### 1. 헤드리스 오디오 처리
- **가상 오디오 디바이스**: PulseAudio dummy driver 또는 ALSA loopback
- **직접 스트림 처리**: FFmpeg의 `-f null` 옵션으로 디바이스 없이 처리
- **메모리 버퍼링**: 디스크 I/O 최소화를 위한 인메모리 버퍼

### 2. 스케일링 전략
- **수평 확장**: 녹음 워커 컨테이너 스케일 아웃
- **작업 분산**: Redis를 통한 작업 큐 분산
- **스토리지 티어링**: 핫/콜드 데이터 분리 저장

### 3. 신뢰성 보장
- **장애 복구**: 녹음 중단 시 자동 재시작
- **데이터 무결성**: 청크 단위 저장 및 체크섬 검증
- **백업 전략**: 정기적인 메타데이터 백업, 오디오 파일 복제

### 4. 보안 고려사항
- **인증/인가**: JWT 기반 인증, RBAC 권한 관리
- **API 보안**: Rate limiting, CORS 설정
- **데이터 암호화**: 저장 시 암호화, 전송 시 TLS

### 5. 성능 최적화
- **스트리밍 처리**: 전체 파일 로딩 대신 스트리밍
- **캐싱 전략**: Redis 캐싱, CDN 활용
- **비동기 처리**: FastAPI의 async/await, Celery 백그라운드 작업

## 확장 가능한 기능

### Phase 1 (MVP)
- 기본 녹음/재생 기능
- 웹 대시보드
- 스케줄 관리
- 파일 관리

### Phase 2
- AI 전사 기능
- 고급 검색
- 플레이리스트
- 사용자 관리

### Phase 3
- 팟캐스트 RSS 생성
- 소셜 공유 기능
- 모바일 앱
- 실시간 스트리밍 릴레이

### Phase 4
- 다중 서버 동기화
- 광고 자동 제거
- 음악/음성 자동 분류
- API 외부 공개

## 개발 로드맵

1. **Week 1-2**: 프로젝트 초기 설정, Docker 환경 구성
2. **Week 3-4**: 백엔드 코어 기능 (녹음, 스케줄링)
3. **Week 5-6**: 프론트엔드 기본 UI 구현
4. **Week 7-8**: 통합 테스트, 버그 수정
5. **Week 9-10**: AI 기능 통합
6. **Week 11-12**: 성능 최적화, 배포 준비

## 성공 지표

- **안정성**: 99.9% 업타임, 녹음 실패율 < 0.1%
- **성능**: 동시 10개 이상 스트림 녹음 가능
- **저장 효율**: 압축률 70% 이상
- **사용성**: 대시보드 로딩 시간 < 2초
- **확장성**: 일일 100GB+ 녹음 처리 가능

## 라이선스 고려사항

- FFmpeg: LGPL/GPL 라이선스 준수
- 오픈소스 컴포넌트 라이선스 검토
- 상업적 사용 시 라이선스 정책 수립